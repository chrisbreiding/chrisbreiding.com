{
  "name": "chrisbreiding.com",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && vite build",
    "deploy": "echo 'Deploying via netlify' && git push origin $(git rev-parse --abbrev-ref HEAD):production",
    "preview": "npm run build && vite preview --port 8888",
    "start": "vite --port 8888"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
